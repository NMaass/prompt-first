export function App() {
  return (
    <main className="shell">
      <section className="card" aria-labelledby="starting-title">
        <p className="eyebrow">Prompt First workspace</p>
        <h1 id="starting-title">Your product is starting.</h1>
        <p>The autonomous builder will replace this starter with the mission product and publish a preview when it is useful.</p>
      </section>
    </main>
  )
}
